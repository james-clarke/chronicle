/target
site/img/src/

# Build logs from `cargo install` runs.
install-*.log

# Editor and tool-state ignores (`.aider*` and friends, kept in .git/info/exclude
# here) would otherwise swallow this: it is an aider *fixture*, test data the
# session-format test reads, not aider's own state. A repo .gitignore beats
# .git/info/exclude, so the negation has to live here.
!fixtures/sessions/aider/repo/.aider.chat.history.md
